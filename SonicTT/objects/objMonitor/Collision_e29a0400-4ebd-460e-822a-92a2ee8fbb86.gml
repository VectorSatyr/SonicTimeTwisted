/// @description  Handle landing

// vertical limit only
if abs(vspeed)>0 while place_meeting(x, y, other) y -= sign(vspeed);

// stop falling
if place_meeting(x, y+image_yscale, other) {vspeed = 0; gravity = 0;}

