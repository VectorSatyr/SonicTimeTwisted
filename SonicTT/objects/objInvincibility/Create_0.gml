action_inherited();
/// Initialize
var i;

// properties
spacing = 2;
angle = 0;
angle2 = 0;
flip = false;

// initialize
for (i=0; i<3; i+=1) {px[i] = floor(x); py[i] = floor(y);}

// start timer
alarm[0] = 1200;

